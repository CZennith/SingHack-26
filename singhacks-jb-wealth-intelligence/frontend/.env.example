# Backend base URL. Leave empty while using the prototype fixtures.
VITE_API_BASE_URL=""

# mock keeps the UI on the prototype fixture repository. The future live mode
# will consume the typed connector contracts in src/services/.
VITE_CONNECTOR_MODE="mock"

# Public, non-secret client settings only. Keep credentials server-side.
VITE_DATA_AS_OF="2026-08-26"
